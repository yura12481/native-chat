import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  errorMessageContainer: {
    width: '100%',
    borderTopWidth: 3,
    borderTopColor: 'red',
  },
  errorMessage: {
    marginBottom: 3,
    fontSize: 14,
    fontWeight: 'bold',
    color: 'red',
  },
});
